apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-service
  namespace: smartsite
spec:
  replicas: 2
  selector:
    matchLabels:
      app: team-service
  template:
    metadata:
      labels:
        app: team-service
    spec:
      containers:
      - name: team-service
        image: smartsite/team-service:latest
        ports:
        - containerPort: 3003
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: team-db-url
        - name: PORT
          value: "3003"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: team-service
  namespace: smartsite
spec:
  selector:
    app: team-service
  ports:
  - protocol: TCP
    port: 3003
    targetPort: 3003
  type: ClusterIP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: team-service-hpa
  namespace: smartsite
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: team-service
  minReplicas: 2
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
